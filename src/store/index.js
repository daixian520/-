import Vue from "vue";
import Vuex from "vuex";
import { stat } from "fs";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // orderList:[],
    // menuList:[]
  },
  mutations: {
    // orderList(state,data){
    //   state.orderList=data;
    //   sessionStorage.setItem("list",JSON.stringify(state.orderList))
    // }
  },
  actions: {},
  modules: {}
});
